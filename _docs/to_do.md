# Wimbi — To-Do Tracker

**Purpose:** Self-tracker for progress against `delivery_strategy.md`. This doc changes constantly — check items off as they're actually done, update **Last updated**, and leave `delivery_strategy.md` itself alone (that one's the stable reference).

**Last updated:** 2026-09-09
**Current phase:** Phase 0

---

## Phase 0 — Now (small team, pre-MVP)
- [ ] Provision QA / UAT / PRD environments (AWS)
- [ ] Set up Jira project with epics matching the feature catalog
- [ ] Define branching strategy and CI pipeline skeleton
- [x] Write unit + integration tests as features are built (not after)
- [x] Start the RBAC negative-test suite alongside RBAC implementation, not after
- [ ] Draft an ADR for each decision already made in `business_requirements.md` §9

## Phase 1 — Internal MVP → UAT gate
- [ ] Define UAT sign-off owners per persona
- [ ] Build out E2E tests for the MVP's core flows (search, timeline, login/RBAC)
- [ ] Run a first informal security pass (dependency scan, secrets audit)
- [ ] Start a Data Protection Impact Assessment with Legal — long lead time, start early
- [ ] Usability test with actual pilot candidates, not just the team

## Phase 2 — External rollout (call center first)
- [ ] Formal UAT sign-off completed and documented
- [ ] Access review process running on a schedule
- [ ] Incident response process defined (even lightweight)
- [ ] Feature-flagged rollout plan per persona group
- [ ] First real penetration test

## Phase 3 — Bigger team / handover
- [ ] RACI review with incoming team
- [ ] All informal decisions formalized or retired
- [ ] Full playbook adoption (Scrum ceremonies, CAB, formal SLAs)
- [ ] Country-specific compliance review completed for every live country

---

## Log
Short dated entries when something meaningful gets checked off or a phase changes — not a full changelog, just enough to see momentum at a glance.

- 2026-08-31 — Tracker created, nothing started yet.
- 2026-08-31 — Scaffolded the Next.js app and shipped the first vertical slice: dev-login (Auth.js, stand-in for Keycloak) → farmer search → profile → Journey Timeline, RBAC-scoped by country, running on mock fixture data behind a swappable `DataSource` interface (real Snowflake client built but not wired to live credentials). Unit tests (RBAC negative cases + mock data source) and an E2E flow (Playwright) pass; build and lint clean. `git init` deferred one more step — repo not yet initialized.
- 2026-09-01 — Rebuilt on Django + PostgreSQL per `_docs/architectural_decisions.md` (ADR-001 through ADR-006): Next.js prototype removed, Django backend (accounts/analytics_mirror/farmers apps) built fresh with the same RBAC contract, 16 backend tests passing. Loaded real Malawi data (~1.3M farmers, ~1.5M journey events, ~1.7M bridge/lineage rows, ~10k SF employees) via a chunked Postgres `COPY` loader — row counts match the Q2 2026 Entities Project Report exactly. Verified end-to-end against real data (search → profile → journey → cross-country RBAC block). **Open and blocking real RBAC**: SuccessFactors department names don't map cleanly to Wimbi's role vocabulary (no "Call Center" or "Data Team" department exists) — needs a decision, not a guess. Reflex frontend not started yet.
- 2026-09-01 (later) — ADR-007: consume pre-built REPORTING views for computed metrics, mirror DIMENSIONS directly, raw FACTS only for genuinely new cuts. Folded in `sales_detail.csv` (real `V_SALES_DETAIL`, 5M/12.78M Malawi rows) as a new `SalesLine` model + `GET /api/farmers/<id>/sales/` endpoint, enriching the Journey feature with product/quantity/field-officer/site detail — 18 tests passing, verified against a real farmer. Hit and worked through a real scale incident along the way: the full 5M-row load twice crashed with `MemoryError` (once taking Docker Desktop's WSL2 VM down with it) under this machine's actual memory pressure (host was down to ~0.6GB free with Docker + the existing Superset stack + Postgres all running) — recovered by finishing the load in smaller fresh-process passes against remainder files rather than one continuous run. All 5,000,000 rows loaded and verified. Concrete, lived evidence for ADR-003's incremental-loading concern, not just a theoretical one.
- 2026-09-02 — ADR-008: elastic, admin-editable role-assignment module. New `accounts.RoleAssignmentRule` (department + optional WorkLocation condition → Django Group(s) + country-scope override) and `assign_roles` management command, seeded via a data migration. Ran against all 10,074 real SF employees — every resulting group count matches the mapping exactly (Field Officer 4,034, Field Supervisor 1,968, Business Ops 241, Program Executive 29, Data Team 114 with `ALL` country scope, Business/Program User 2,013, Gamma 1,675 fallback). "Admin" is structurally unassignable via the rule engine (an `m2m_changed` guardrail, fires regardless of entry point) and the sync only ever touches groups the rule table owns, so a manually-granted Admin membership always survives a re-run — tested. **Same day, follow-up**: learned Call Center staff sit inside "Business Operations" with no field yet to distinguish them — `group` became a `groups` M2M so one rule can grant both Business Ops and Call Center at once (both now correctly at 241 members) rather than leaving Call Center permanently empty; revisit the split once a distinguishing field (e.g. JobTitle) exists. 30 tests passing. **Still open**: dev personas (`accounts/dev_users.py`) stay in place alongside real SF-backed users for now — wiring the live login/session flow to prefer real users is a deliberate follow-up, not done in this pass.
- 2026-09-02 (later) — ADR-010: frontend settled as Django + HTMX, Reflex dropped. Reached after three deliberate rounds of pushback (read/write shape vs. Odoo, Superset's own React/TS frontend, DataHub's React/TS+GraphQL frontend, and a forward check against the next roadmap feature — the bulk uploader's column-mapping UI) rather than a first-pass pick. Named tripwire for revisiting: a future screen needing a real interactive relationship/lineage graph or a dashboard *builder* (not this MVP) would be the signal, not a hypothetical.
- 2026-09-02 (later) — Built the v1 UI on ADR-010: `django-htmx` wired in (middleware + `request.htmx`), `accounts/rbac.py:scope_queryset` for DB-level RBAC filtering on aggregates, an `accounts.context_processors.session_user` context processor so every template gets the current user for free. Pages: `/login/` (real CSRF, no more `@csrf_exempt`), `/dashboard/` (4 stat tiles + a Chart.js bar chart, colors/marks from the `dataviz` skill's validated default palette — sequential blue, since it's one series by category, not multiple), `/search/` (HTMX active-search, `hx-trigger="keyup changed delay:300ms"`), `/farmers/<id>/` (profile renders immediately, Journey Timeline and Sales History lazy-load via `hx-trigger="load"`), `/logout/`. All additive to the existing DRF JSON API under `/api/` — same underlying RBAC/session functions, zero changes needed there. 17 new tests (50 total passing), plus an end-to-end smoke test against the real dev DB (login → dashboard → search → profile → journey/sales partials → cross-country 403 → unknown-farmer 404 → logout), all confirmed working. **Not verified in an actual browser** (no browser tool in this environment) — the HTTP/RBAC/data layer is confirmed end-to-end via Django's test client, but the visual styling itself hasn't been eyeballed yet; worth a real look before calling the design final. Bulk Data Mapper/Uploader (ADR-004) is next, as its own plan.
- 2026-09-02 (later still) — First real browser testing session found two genuine bugs, both fixed: (1) clicking "Search" from the sidebar (an `hx-boost`ed nav) rendered a blank page — `request.htmx` was true for both that boosted navigation and the search input's own live-search AJAX call, and the view returned the (near-empty) results partial for both; fixed by checking `request.htmx.target == "search-results"` specifically. (2) More serious: a broad query (`q=g`) against the real 1.3M-farmer table matched a huge fraction of names, `scope_farmers` materialized the whole unbounded match set into Python, and the rendered response hit **113MB** — which crashed the dev server outright (`BrokenPipeError`, process exit). Same class of at-scale problem as ADR-003's CSV-loader `MemoryError` incident, this time in a path real users hit directly. Fixed: both the JSON and HTML search endpoints now share `_search_farmers_queryset()`, pushing the country scope AND a hard 50-result cap down to the database via `scope_queryset` + a SQL `LIMIT`, plus a 2-character minimum query length. Verified against real data post-fix: `q=g` dropped from 113MB to ~3KB. 54 tests passing (3 new regression tests for this exact incident). Full detail in ADR-010's 2026-09-02 update. Both bugs found within minutes of the first real click-through — confirms the earlier caveat that HTTP-client testing alone wasn't sufficient.
- 2026-09-02 (later still) — Added `_docs/jira-backlog.md` (Epic/Story inventory across all 19 catalog features plus 3 platform epics for the infra work — Application Platform, ANALYTICS Mirror, RBAC & Identity — each story status-tagged Done/In Progress/Not Started/Blocked/Parked against what's actually built) and `_docs/uat.md` (browser-based acceptance tests for everything shipped so far, weighted toward judgment calls only a human can make — real-data plausibility checks, role-mapping against one's own real SF identity, visual/UX impressions — not duplicating the automated suite). Neither replaces this tracker; `jira-backlog.md` is the structured full-scope inventory, `uat.md` is for the user to actually run.
- 2026-09-02 (later still) — UAT session #1: real farmer profile (`MW-00008737`, Joshua Bruwayo, Kobo tree sales) surfaced a real data-display gap — every Sales History row showed "— MWK" for Total despite real quantities, and the profile's "Total sales" stat was also "—". Investigated directly against the mirrored data (not guessed): `total_price_lcy`/`unit_price_lcy` are genuinely null for this farmer's lines, but `total_order_price_lcy` (order-level) is populated and was being silently dropped. Fixed: the sales table now falls back to it, labeled "(order total)" so it's never mistaken for a per-line figure; the JSON API exposes it as a new `orderTotalPriceLcy` field rather than silently substituting server-side. The profile's "Total sales"/"Repayment rate" dashes turned out to be *correct*, not bugs — this farmer has zero loans (no repayment rate applies) and the upstream `V_CLIENT_REACH` rollup is itself null despite real sale activity, a genuine ANALYTICS-side gap now documented in the ADR appendix rather than papered over with a locally-computed number (would've violated ADR-007). 55 tests passing (1 new regression case, plus a new Kobo-style fixture line in `seed_data.py`). No other issues found in this first UAT pass.
- 2026-09-03 — Decided to populate the remaining 4 REPORTING views via a direct Snowflake→Postgres transfer in DBeaver instead of extending the CSV loader (which already had a real memory incident, ADR-003). Wrote `DESCRIBE VIEW` + sample-row queries into `data/raw/snowflake.sql` for `V_LOAN_PORTFOLIO`, `V_PROGRAM_SUMMARY`, `V_REPAYMENT_ANALYSIS`, `V_FO_PERFORMANCE`; user ran them and pasted results back. Added matching Django models (`LoanPortfolio`, `ProgramSummary`, `RepaymentTransaction`, `FOPerformance`) to `analytics_mirror/models.py`, registered them in `seed_data.py`'s `MIRROR_MODELS`, and created their empty tables in the real dev Postgres via a new `ensure_analytics_tables` command — deliberately schema-first, so DBeaver does a data-only load into tables shaped exactly like our models expect, rather than auto-generating DDL itself (avoids a real risk: Snowflake's uppercase column convention could otherwise produce columns our lowercase-expecting ORM can't find). Two things worth chasing later: `V_REPAYMENT_ANALYSIS` has a genuine phone column (`REPAYMENT_PHONE`) that could partially unblock Feature 1's deferred phone-search story; `V_FO_PERFORMANCE.FO_ID` is blank in sample data even when other fields are populated. 55 tests passing (additive, nothing broken). Real data load itself is the user's next step, via DBeaver, independent of this session.
- 2026-09-03 (later) — **Bulk Data Mapper/Uploader v1.1 built** (ADR-004/Feature 18, full requirements now in `_docs/bulk-uploader.md`). Design was grounded in real artifacts rather than description: read `new_variable_mapping.csv` (OAF's actual canonical model — 8 entities, ~19 views, ~150 variable rows) and `dims/{Countries,Programs,Systems,MCF}.csv`, then synthesized down to **~45 variables across 8 entities** by dropping the normalized "View" layer (ERD furniture — real uploads arrive flattened) and anything no FACT/DIM consumes (computed aggregates, warehouse stock movement). Two findings materially changed the design: `CROSS_ID` varies by country (NID/APPSHEET_ID/ACCOUNTNUMBER/**none**), so the quality gate requires *an* identifier (`id_type`+`id_value`) not a specific scheme — a hard-coded NID rule would have rejected most of Malawi; and OAF's live Google Form already asks exactly the right funnel questions, so those were ported rather than reinvented. Built: new `bulk_uploader` app (`glossary.py`, `models.py` with `UploadedDataset`/`UploadedRow`, `parsers.py` for CSV/XLSX via built-in `csv` + `openpyxl`, not pandas), a 3-screen flow (funnel+file → map → preview+confirm) reusing `base.html`/HTMX exactly per ADR-010, and exact-name-match mapping pre-fill (real AI suggestions are v1.2+). Quality gate is deliberately **visible, not destructive**: invalid rows are flagged and kept, never silently dropped, and never reject the whole file. 69 tests passing (14 new). Smoke-tested end to end against the real dev DB with a deliberately-broken row — auto-suggest matched 5 of 6 columns including `First Name`→`first_name` and `PHONE`→`phone`. **Also ADR-011**: the alternating Wimbi↔ANALYTICS improvement cadence written up as the explicit operating model. Deferred and named in `bulk-uploader.md`: Sheets/Snowflake connectors, AI mapping, steward approval, the promotion ETL into `ANALYTICS.SOURCES`, `gl_client_id` matching.
- 2026-09-03 (later) — First real UAT on the uploader (Malawi Trees registration file, 1,000 real rows) surfaced two genuine design gaps, both logged for a follow-up pass rather than fixed blind: (1) a real bug — mapping rewrites `UploadedRow.mapped_data` in place, so re-mapping loses the original column-keyed values (explains "samples disappeared" and an all-blank preview); (2) a real design mismatch — the `id_type`+`id_value` pair requirement is long/EAV-shaped, but every real export is wide (a farmer's identifier is one column, e.g. `UNIQUE_FARMERID`, not two). Also flagged: the glossary (~45 variables) is narrower than `Data Collection Template.xlsx`'s real field list, which should set the minimum/optional/nice-to-have floor instead. None of this fixed yet — discussed, not touched, per the user's explicit "don't fix anything, let's chat first."
- 2026-09-03 (later) — User loaded all 5 real ANALYTICS dimension tables (`DIM_COUNTRY`, `DIM_MCF`, `DIM_PROGRAM`, `DIM_SEASON`, `DIM_SYSTEM`) via DBeaver's direct Snowflake-to-Postgres transfer, letting DBeaver auto-create the destination tables this time — which reproduced exactly the risk flagged back when the first 4 REPORTING views were added: uppercase columns (`COUNTRY_ID`, `PROGRAM_OAF_EQ`, ...) that Django's lowercase-expecting ORM can't see. Caught by checking directly rather than assuming. Fixed properly: added `DimCountry`/`DimMCF`/`DimProgram`/`DimSeason`/`DimSystem` models (schemas pulled from the real live Postgres columns, richer than the sample `dims/*.csv` — e.g. real `DIM_PROGRAM` has `CROSS_PROGRAM_ID_TYPE` and `PHONE_QUALITY` as genuine columns, confirming the bulk uploader's scheme-agnostic identity design and the Kobo phone-quality signal are both grounded in the real warehouse, not just the sample export), dropped the 5 uppercase tables, recreated them Django-first via `ensure_analytics_tables`. 69 tests still passing. User is redoing the 5 (small: 10-218 rows each) transfers into the new tables.
- 2026-09-03 (later still) — **`glossary.py` rewritten to use real warehouse names, not the design-document synthesis.** Read the pipeline's actual DDL directly (`entities/database/dimensions.sql`, `facts.sql`, `master_procedure.sql`) rather than continuing to work from `new_variable_mapping.csv` (a design document, not the built system) or the Data Collection Template's natural-language names. This settled several things two AI-assisted mapping "stretch tests" against real files (`mw_lr26 registration data.csv`, `mw_seedlings_distribution_data.csv`) had flagged as genuinely ambiguous: `field_officer`/`shopkeeper`/`nursery_manager` are three real, distinct `DIM_PEOPLE` roles (not one generic "served_by"); `EPA`→`sector` and `NURSERY_SITE`→`lowest_loc` are confirmed exactly by `DIM_LOCATION`'s own Kobo-branch SQL; `DIM_CLIENT` has three genuinely separate identity columns (`national_id`/`account_number`/`fineract_id`), confirming the earlier EAV (`id_type`+`id_value`) design was wrong twice over — wrong shape *and* wrong requirement (a client's personal id isn't the same thing as `source_client_id`, the universal lineage id). `FACT_PURCHASE` turned out to exist for real, so Purchase is no longer unverified — real fields include a genuine buyback-specific split (`loan_settlement_lcy` + `payout_lcy`). Also relaxed "location is required" — the actual DDL never enforces it (`V_SAVINGS` has no district at all); required is now scoped to exactly what was asked for, a client identifier and a per-entity lineage/transaction id. Every variable now carries a description. 69 tests updated and passing (some deliberately re-target what a naive name-match can and can't catch, now that variable names are the warehouse's own — e.g. "Transaction ID" matches `source_transaction_id` via its label, but "UNIQUE_FARMERID" matches nothing, which is honest, not a bug).
- 2026-09-03 (later still) — Stretch-tested AI-assisted mapping against two real Kobo Trees files. Found a real structural gap, not a naming one: `mw_seedlings_distribution_data.csv` is wide/pivoted (one column per tree species holding a quantity — `A_LEBBECK_SEEDLINGS`, `F_ALBIDA_SEEDLINGS`...), while `FACT_SALE` is long-grain (one row per product). Decided **not** to unpivot at Wimbi's capture stage — pull the file as-is, unpivot when the real `SOURCES` view for this dataset gets built (a manual pipeline step, matching how the rest of `entities/database` already works). Follow-up not yet built: the mapping UI currently only has "map to a variable" or "drop" — needs a third "keep as raw column" option so pivoted columns aren't silently lost between now and whenever the promotion ETL exists.
- 2026-09-03 (later still) — Real bug from live use: the mapping dropdown was scoped to the funnel's single "primary" entity (e.g. Distributions → only Sale variables), but real uploads are routinely multi-entity — the actual `mw_seedlings_distribution_data.csv` carries Client, Location, People, and Sale columns in one file, and `FARMER_NAME` had nowhere to go. Fixed: the dropdown now shows every entity's variables, grouped by `<optgroup>`, not restricted to one (`glossary.all_variables_grouped()`); the funnel's chosen entity still decides what's *required* for the quality gate, only the dropdown's contents were unrestricted. Also added the missing back link on the mapping page (none existed before — a real, if smaller, UX gap). Verified against the real seedlings file: `FARMER_NAME` now maps to Client's "Full name". 69 tests still passing, unchanged.
- 2026-09-03 (later still) — Added back links to all 3 upload steps (funnel, map, preview) for consistency. Then a real gap: the map page's "Back to uploads" only reached the list, with no way to actually correct the funnel answers (country/program/data type) without abandoning the draft and re-uploading the file. Built `dataset_edit` — a real edit view for those fields (file and any existing mapping stay untouched), linked from the mapping page. Also made **Data Type optional** at upload time (was blocking uploads for no strong reason); left blank, entity defaults to `client` via a new `glossary.entity_for_data_type()` helper, editable later through the same edit view. Changing data type resets the dataset to draft status, since it changes what's required for the quality gate — a stale "saved" status validated against the wrong entity would be a real, if quiet, correctness bug. 72 tests passing (3 new).
- 2026-09-03 (later still) — Mapping page overhaul, several requests at once: (1) **glossary restructuring** — `field_officer`/`shopkeeper`/`nursery_manager` moved from Sale into People (matches `DIM_PEOPLE`'s own real sourcing), and People's generic `full_name` removed as redundant now that the three specific roles exist there (Client keeps its own `full_name`, untouched). (2) **`pivoted_product`** — a new Sale variable that many *columns* can map to at once, each preserved as its own `{column, value}` entry rather than the last one overwriting the rest (`parsers.MULTI_VALUE_VARIABLES`) — the real answer to the wide/pivoted seedlings file's unpivot problem: capture every product-quantity column now, actual reshape still deferred to the SOURCES-promotion step as already decided. (3) **Mapping page rebuilt**: the ~80-option native `<select>` per column (650KB page against the real seedlings file) replaced with a client-side searchable combobox capped at 10 results, a per-row mapped checkmark, an A–Z sort button, and row checkboxes with a bulk "clear mapping for selected." Page size against the same real file dropped to 52.5KB. 77 tests passing (5 new, including a full map→save→preview round trip through `pivoted_product` and a check that its list value renders as text rather than a raw Python repr).
- 2026-09-03 (later still) — Four more real fixes from live use of the mapping page: (1) **The "samples disappear" bug, actually fixed this time** — `UploadedRow` now has a permanent `raw_data` field (keyed by source column, never touched again) separate from `mapped_data` (a derived result, recomputed fresh from `raw_data` on every mapping save, never compounded on top of the previous pass). Existing draft datasets backfilled from their still-original `mapped_data`; the 3 that had already gone through a mapping save had already lost their raw values before this fix existed and need a fresh re-upload. (2) **"Client ID (lineage)" showing up everywhere with no clear reasoning** — `source_client_id` (and other lineage fields) intentionally exist on 5 different entities with identical meaning; the option list wasn't deduped, so the same field appeared 5 times, each tagged with an almost arbitrary single entity (whichever was built last won a JS lookup keyed only by name). Fixed: one entry per name, tagged with every entity it actually applies to (e.g. "Client / Sale / Purchase / Loan / Payment"). (3) **Sortable like a spreadsheet** — column headers (Mapped / Your column / Maps to) are now click-to-sort, click again to reverse, replacing the single "Sort A-Z" button. (4) **Live required-fields checklist** — the static "Required for X: ..." sentence is now a live checklist with ✓/☐ per field, updating immediately as columns get mapped or cleared. 80 tests passing (3 new, including a direct regression test for the raw_data bug and a dedup check for the options list).
- 2026-09-03 (later still) — **Ignore state added**, prompted directly by a real mistake caught on screen: `TOTAL_SEEDLINGS` (a checksum column) was mapped to "Full name" — stale leftover from earlier testing, invisible because there was no way to distinguish "reviewed and decided this doesn't matter" from "haven't looked at it yet." New `UploadedDataset.ignored_columns` field (persists across page reloads, unlike a client-only flag would); a per-row "Ignore" checkbox for one-off decisions plus bulk "Ignore selected"/"Un-ignore selected" actions alongside the existing "Clear mapping for selected"; ignored rows grey out and can't simultaneously hold a mapping (ignoring clears it, taking precedence over any stray leftover selection server-side too). Also turned the required-fields checklist into a responsive grid instead of an inline sentence, per request — and flagged an open question back to the user rather than guessing: Sale/Purchase's "required" is currently just the lineage id, deliberately narrower than the Data Collection Template's original ~11-field bar (which also wanted product/quantity) — a real tension between what the DDL enforces and what a well-formed dataset should have, not yet resolved. 82 tests passing (2 new).
- 2026-09-03 (later still) — Location entity gained `village`, `cell`, `source_location_id` (per user request, deliberately added ahead of the real warehouse — `DIM_LOCATION` confirmed via direct DDL read to have exactly 11 columns, none of these three — tracked as **GAP-004** in `upstream-gaps.md` for the user to add for real). `loc_type` moved out of Location entirely and into a new funnel-level field (`UploadedDataset.location_type`) — a single upload is virtually always one place-type (nursery/site/shop/warehouse/online), same reasoning as Country/Program. **New: a `tier` classification on every `Variable`**, deliberately independent of the existing strict `required` save-gate — restores the Data Collection Template's original Required/Nice-to-have/Optional bands (e.g. Sale's `product_name`/`quantity`/`unit_price_lcy` are tier="required" for the completeness *scorecard* without being required=True for the save *gate*, since real Kobo Trees sales genuinely have no pricing). Mapping page now shows: the narrow save-gate checklist (unchanged), a new full-glossary progress tracker (required + nice-to-have tiers, ~34 fields across all 8 entities, live-updating), and a live mapping percentage. Funnel forms (`upload_new`, `dataset_edit`) changed from a 3-column grid to a single stacked column, per request. Table columns reordered so "Maps to" sits next to "Your column," with "Sample values" pushed last. 86 tests passing (4 new).
- 2026-09-02 (later still) — Corrected the GAP-001 fix above after the user's own domain context: repeating `total_order_price_lcy` per line (the first fix) was itself misleading — it's identical across every line of an order, so showing it 12 times reads as 12 separate totals, not one. Reworked: per-line Total stays an honest "—"; lines missing a price but sharing an order total are now summarized **once per order** (grouped by `source_order_id`) in a note above the table. Learned the real root cause directly from the user: Kobo datasets often arrive with no unit prices at all; `revenue_lcy`/`revenue_usd` exist as a prorated stand-in for Superset's total-revenue reporting, but the actual numbers found (~62 MWK summed vs. a 49,088 MWK order total) don't obviously reconcile — flagged as an open question rather than guessed at. Also confirmed via direct schema introspection: `V_CLIENT_REACH` has no USD/FCY-equivalent total column today, so "show both LCY and FCY" isn't buildable without an upstream addition. All three findings written up in a new **`_docs/upstream-gaps.md`** (GAP-001/002/003) — a dedicated tracker for data-layer issues the user will address at the source, separate from this app-focused tracker. 55 tests passing (1 test updated to match the corrected behavior).
- 2026-09-03 (even later still) — Removed the "(lineage)" label suffix from required-field descriptions in `glossary.py` (the description text already explains the why; the suffix was confusing, not clarifying — per user feedback). Added a real, switch-based **dark mode**: `templates/base.html` now defines a `.dark`-scoped override of every CSS custom property (palette from the `dataviz` skill's documented dark values), a pre-paint `localStorage` check to avoid a flash of the wrong theme, and a sidebar toggle that persists the choice and reloads (so the dashboard's Chart.js colors and a handful of hardcoded status hexes, both set once at load time, pick up the new theme too). 7 template locations' hardcoded `#006300`/`#b3261e`/`#fdecec` swapped for `var(--success)`/`var(--error)`/`var(--error-bg)`. 86 tests passing, unchanged (a styling-only change).
- 2026-09-03 (even later still) — **Removed "Data Type" entirely, replaced with an explicit entities checklist** (first of four approved save-gate changes, built one at a time per the user's request for UAT between each). The old single-select "Data Type" only ever implied one entity by guesswork (`entity_for_data_type`), which under-served real multi-entity files. `UploadedDataset.entity` (single) is now `entities` (a JSONField list) — an uploader checks one or more of the 8 entities directly, and that list is the actual save gate: `glossary.required_variables_for_entities()` unions each checked entity's own required lineage field, and the mapping dropdown/progress tracker are now scoped to just the checked entities (tighter than the previous "show all 8 always"). Migration backfills every existing dataset's old single `entity` value into the new `entities` list — verified against the real dev DB (12 real datasets from earlier UAT sessions, all backfilled correctly, e.g. `mw_seedlings_distribution_data.csv` rows carrying `["sale"]`). At least one entity is now required at upload (previously Data Type was optional) — without it the save gate would have nothing to check. 87 tests passing (2 new, 1 rewritten, 1 removed). Not yet visually browser-tested (no browser tool in this environment) — verified via the full Django test-client flow plus a direct dev-DB query confirming the migration backfill; worth a real look before UAT sign-off. Next up (per the agreed order): funnel dropdowns sourced from real `DimCountry`/`DimProgram`/`DimSystem`.
- 2026-09-03 (even later still) — Two mapping-page requests from live use: (1) **resizable columns** — the mapping table's headers now have a drag handle on their right edge (`table-layout: fixed` + a `<colgroup>` of starting widths + JS mousedown/mousemove resize), since "Sample values" defaulted wider than most real sample text needed. (2) **Location's `lowest_loc` split into named channels** (`nursery`/`site`/`shop`/`warehouse`), mirroring `DIM_PEOPLE`'s own field_officer/shopkeeper/nursery_manager pattern — a single dataset can genuinely carry more than one place-type at once (e.g. both Site and Shop transactions), so one generic field could only ever hold whichever channel happened to be mapped last. Tier set to nice-to-have, not required, for the same reason the three People roles are — only the channel(s) actually present in a given file apply. Which one is DIM_LOCATION's actual `lowest_loc` for a row is a promotion-time (v1.2+) decision driven by the funnel's existing `location_type` answer, not re-asked at mapping time. 87 tests passing, unchanged (no test referenced the old `lowest_loc` name directly).
- 2026-09-03 (even later still) — **Funnel dropdowns switched to real ANALYTICS dims** (item 1 of the original 4-item save-gate list). `Country`/`Program` no longer hardcoded lists in `glossary.py` — `bulk_uploader.views._funnel_dims()` queries live `DimCountry`/`DimProgram`/`DimSystem` (already mirrored) and builds a cascading Country→Program→Source system funnel, client-side JS filtering one embedded JSON payload rather than an HTMX round-trip per selection. Real discovery along the way: `program_oaf_eq` alone can't be the cascade key into `DimSystem` — Kenya has two distinct "Retail" programs ("Asili - Cash (Walk-in)"/"(App)") sharing one oaf_eq code; `program_local_name` is the correct join key and matches `DimSystem`'s own rows exactly. New: **Source system** is a first-class funnel question (`UploadedDataset.source_system`), and when set it shows on the mapping page as a locked, uneditable banner and gets broadcast into every row's `mapped_data` automatically — mirroring `SELECT 'KOBO' AS source_system` applied to the whole file, since a real upload almost never carries its own per-row source_system column; an explicit per-row mapped value still wins if genuinely present. Every dropdown below Country (Program/Source system/Location type) got an "Other…" free-text escape hatch whose typed value becomes the real stored value, not a literal "Other" — `LOCATION_TYPES` dropped "Other" as a 6th fixed choice to use the same mechanism. Added small, deliberately simplified `DimCountry`/`DimProgram`/`DimSystem` fixture rows to `analytics_mirror/seed_data.py` matching the existing test suite's own literals (RW/Core, MW/Trees, KE/Retail) so no test needed to change for this. 87 tests still passing. Verified live against the real dev DB: cascade returns real Malawi (5 programs)/Kenya (5, including the two Retail variants) data correctly. Deliberately deferred without building: a `DimSeason`-backed Season/Year dropdown (same mechanism, flagged to the user as a natural low-cost follow-on rather than built unasked).
- 2026-09-03 (even later still) — **Season/Year dropdowns + synthetic composite keys** (items 4 and 3 of this session's running save-gate work). Operational year is now a plain select (2015–current year, descending); Season cascades off Country via real `DimSeason`, showing only the **3 seasons closest to today** for that country (`views._closest_seasons` — 0 distance for the current season, otherwise the gap to the nearer edge; verified against the real dev DB for Malawi: LR26/SR26/LR27, i.e. the just-ended, current, and upcoming seasons). Both optional; editing an old draft preserves a stored season that's since aged out of the "closest 3" window rather than dropping it. Added a small, deliberately date-relative (not hardcoded-year) `DimSeason` fixture to `analytics_mirror/seed_data.py` so this stays testable indefinitely. **Synthetic composite keys**: for a required lineage field with no single clean source column, the mapping page now lets an uploader combine 2+ of their own columns into an MD5-hashed composite key (`parsers.build_synthetic_key`, normalized trim+uppercase before hashing) — mirrors `DIM_LOCATION`/`DIM_PEOPLE`'s real MD5-keying and `BRIDGE_CLIENT_SOURCE_IDS`'s own fallback to composite matching. Tagged "synthetic" on the preview page, using the same dataset-level-fact tagging concept as the `source_system` broadcast; an explicit real column mapping still wins over a synthetic one if present, and an all-blank row correctly still fails the gate rather than getting a manufactured hash. 96 tests passing (9 new). Verified live against the real dev DB.
- 2026-09-03 (even later still) — **Mapping-page polish round, 7 items from live UAT feedback on the first real full save** (1,000-row Malawi registration file, screenshots reviewed together). (1) Pivoted-product columns now auto-satisfy `product_name`/`quantity` in the checklist — a pivoted column's own name is the product and its value is the quantity, so mapping one shouldn't still show those two as outstanding. (2) New **Stats column** on the mapping table: % unique and % blank per source column, computed over every row (not just the 5-row sample) — a quick cardinality/completeness signal before committing a mapping or a synthetic key, with blank counted as a real value so an all-blank column correctly shows near-zero uniqueness rather than a misleading high one. (3) Once a real column is mapped to a variable, that variable drops out of every *other* column's search — a 1:1 mapping is the normal case, and offering an already-claimed variable elsewhere just invited duplicate-mapping mistakes; multi-value variables (pivoted_product) stay available everywhere, by design. (4) **Collapsible sidebar** with inline SVG icons (Dashboard/Search/Upload/Log out — none existed before) — persists via `localStorage` like dark mode, same pre-paint-script technique to avoid a flash of the wrong state; this is app-wide (`base.html`), not just the uploader. (5) Funnel's "Any comment?" reworded to "Tell us what this data upload is about, in a nutshell" — same on both the upload and edit forms. (6) Synthetic-key section is now collapsed by default (auto-opens if the dataset already has a configured combo), each field's own "Build from columns" line disappears once a *real* column mapping satisfies it (staying visible if only synthetic-satisfied, so the uploader's own checkboxes are never stranded), and clicking "Validate and preview" with something still missing blocks once — expanding and scrolling to the relevant field's synthetic option — then lets a second click through regardless, matching the app's existing "visible, not destructive" stance rather than hard-blocking a save real Kobo Trees data sometimes genuinely can't complete. (7) Preview page's flagged-rows banner no longer lists every required field name (already itemized per-row further down) — just says a row is missing at least one required field. 100 tests passing (4 new, covering the stats computation; the JS-only behaviors — dropdown exclusion, synthetic collapse/reveal, submit-nudge, sidebar collapse — aren't covered by the Django test-client suite and need a live look, same as the searchable combobox and sort features before them). Variable-list review (loan_product_name's cross-entity placement concern) deliberately deferred to its own pass, per the user's own sequencing.
- 2026-09-03 (even later still) — **Glossary review + a real in-app Glossary page.** Reviewed the full ~99-field variable list together (pulled into a filterable page for the user to scan) and found one real placement issue: `loan_name`/`loan_product_name` looked like a naming coincidence between Loan and Sale, but a direct DDL read (`entities/database/sources_views.sql`) showed they're genuinely different real columns — `FACT_LOAN.loan_name` sources from Fineract's `LOANNAME` (a per-account label per the DDL alone), while `FACT_SALE.loan_product_name` sources from the distinct `FINERACT_PRODUCT_LOAN_NAME`. Flagged rather than silently renamed; the user confirmed `LOANNAME` is, in practice, the loan product's name, so `glossary.py`'s Loan entity was renamed `loan_name` → `loan_product_name` — logged as **GAP-005** since the DDL itself still doesn't document that fact for the next person reading it cold. Also logged, deliberately not acted on: the user's own observation that "Loan" may be the wrong entity name given OAF is fundamentally sales-led — "Credit" might fit better; left alone per explicit request. Then built the review artifact as a **permanent in-app page** (`/glossary/`, `bulk_uploader.views.glossary_page`) rather than leaving it a one-off Claude artifact that would go stale — reuses `base.html` exactly (dark mode, tokens) rather than a bespoke theme, open to any logged-in user (not gated behind the uploader's own Admin/Data Team access, since it's schema reference, not farmer data), with the same live search + shared/single-entity/save-gate filter chips as the review artifact. Added as a permanent sidebar nav item with a new book icon. 103 tests passing (5 new).
- 2026-09-09 (later) — **Wimbi is live on AWS.** Full guided provisioning in AWS CloudShell (root switched to a real IAM user first thing; real farmer PII found and scrubbed from git history via `git filter-repo` before the first-ever push, see `aws_migration.md`) — the whole `infra/aws/` Terraform stack applied clean (VPC, RDS Postgres 17, ECS Fargate, ALB, ECR, IAM, Secrets Manager), real image built and pushed from CloudShell, ALB health check green. Django migrations and `ensure_analytics_tables` both run cleanly via one-off `ecs run-task` overrides (`exitCode: 0` each), no ECS Exec setup needed for either. Real, working instance at the ALB's DNS name. Along the way: chose Single-AZ RDS over Multi-AZ after confirming it doesn't affect the actual usage pattern (weekly Airbyte batch writes, not continuous live traffic) — cuts the estimate from ~$90/month to ~$65-70/month; fixed a `FreeTierRestrictionError` on backup retention (the account's default AWS "Free Plan" caps it below what was hardcoded); fixed a real AWS API constraint on security group description characters that `terraform validate` caught. **Deliberately paused here** — `bulk_uploader` data migration and the Airbyte VPC-peering step (needs OAF's VPC details) still open, picking back up later rather than rushing them at the end of a long session.
- 2026-09-09 — **Local dev environment hit a real disk fault** (WSL2/Docker virtual disk, host C: drive at 96% full) — Postgres started throwing `Read-only file system` errors, a container restart didn't clear it (`psql` itself failed inside the container with an I/O error), and the disk turned out to have been detached; reattaching it and reconnecting resolved it, DB access confirmed restored, 103 tests still passing. Decided, in response: **move Wimbi to AWS permanently**, not just recover locally. Wrote `_docs/aws_migration.md` (the plan, decisions, and open questions) and built the executable half: `Dockerfile`/`docker-entrypoint.sh`, three additive `config/settings.py` changes (`DJANGO_ALLOWED_HOSTS`, WhiteNoise for static files, a `DJANGO_HTTPS`-gated security-settings toggle), and a full Terraform stack under `infra/aws/` (VPC, RDS Postgres 17 Multi-AZ, ECS Fargate, ALB, ECR, IAM, Secrets Manager) — deliberately public-subnet Fargate behind a security group rather than private-subnet+NAT, saving ~$32/month a single-instance app doesn't need. Real decisions locked in: personal AWS account (not OAF's), permanent (not a stopgap), production-shaped (Option B, not the cheaper single-EC2 option), `analytics_mirror` gets rebuilt fresh via **OAF's own existing self-hosted Airbyte** (as a new destination, not re-copied from local) rather than the ad-hoc DBeaver transfers used so far, and only `bulk_uploader`'s own small tables (`UploadedDataset`/`UploadedRow` — genuinely irreplaceable real work) need an actual data migration. **Not yet run against real AWS** — no credentials/CLI in this environment; `infra/aws/README.md` has the full apply sequence, still blocked on OAF's Airbyte VPC details for the peering step. 103 tests passing throughout, local dev unaffected (every settings change defaults to today's exact behavior).
- 2026-09-09 (end of day) — **Airbyte connected, `analytics_mirror` views built.** Fixed a real subnet-routing bug (RDS's public IP had no internet-gateway route — Terraform can't move an RDS instance's subnet group within one VPC, so the fix was routing the existing private subnet to the IGW instead, gated on `db_publicly_accessible`) and a real Postgres grants gap (`airbyte` role needed database-level `CREATE`, not just schema-level, for Destinations V2's staging schema). Added a dedicated `db_admin_cidrs` security-group rule for direct pgAdmin/DBeaver access, and an optional `enable_adminer` ECS service for a browser-based DB view — both bridge-period, gated, cheap to turn off. Built all 13 `analytics_mirror` views over Airbyte's raw-landed Snowflake data (`infra/postgres/analytics_mirror_views.sql`) instead of a copy/transform step — live pass-through, no staleness window — including a reusable SQL replica of `country_codes.py`'s ISO normalization and `ROW_NUMBER()` surrogate ids for the five tables with no natural key (verified safe against real usage first). Added `public.wimbi_analytics_mapping` — a queryable Wimbi↔Snowflake schema cross-reference (`infra/postgres/wimbi_analytics_mapping.sql`), covering all 21 known real objects. Confirmed against real, previously-documented row counts (`dim_program`=29, `dim_season`=171, `dim_system`=218, `dim_mcf`=7 — exact matches) that the view layer is correct. Split the Snowflake source into three connections by size tier after the first full sync's ~3 hours turned out to be a scheduling problem (small tables queued behind multi-million-row ones), not just raw data volume. Hit and diagnosed a real failure mode: a Full-Refresh-Overwrite stream tried to `DROP TABLE` a raw table with a dependent view and correctly failed — confirmed the fix is switching those streams to Incremental, not enabling Airbyte's `drop_cascade` option (which would silently break the view instead, unattended, with no visible error). **Paused deliberately** with several large-table syncs still running — resuming once they finish and Incremental mode is confirmed viable per-stream.
- 2026-09-02 (later) — ADR-009: login now provisions just-in-time, not in bulk. `accounts.provisioning.get_or_provision_user(email)` is the whole mechanism — resolves against `SFEmployee`/`RoleAssignmentRule` (or `DEV_USERS`) and re-applies group membership + country scope on *every* login, not just the first, using the same "never touch Admin" safety as before. `auth.User` reset to a genuine blank slate (yesterday's bulk-created 10,074 rows deleted); `assign_roles` replaced with a read-only `preview_role_assignments` report. Custom session-cookie mechanism retired in favor of real `django.contrib.auth` (`login()`/`logout()`/`request.user`). Found and fixed a real bug along the way: DRF's empty `DEFAULT_AUTHENTICATION_CLASSES` was silently overwriting `request._request.user` back to `AnonymousUser` on every `@api_view` call, masked until now because the old custom session-cookie approach never actually relied on `request.user`. Verified end-to-end against the real dev DB: dev + real logins, RBAC 403/200 correctly, logout, exactly as many `User` rows created as people who logged in. 33 tests passing.
