# Wimbi — AWS Migration Plan

**Status:** Wimbi is live on a personal AWS account (`eu-north-1`) — full stack applied, migrations run, app serving real traffic. **This is a ~3-week bridge, not the permanent home** — OAF's own DevOps team provides a real domain under `*.oneacrefund.org` (`prd`/`qa`/`uat`) once this migrates into OAF's own AWS account; SSL/domain setup here is deliberately deferred until then rather than buying a throwaway personal domain. Local dev is unaffected either way — everything here is additive (new files, new env vars with safe defaults), nothing about the existing docker-compose setup changed.
**Last updated:** 2026-09-09
**Companion to:** `infra/aws/README.md` (the executable half of this plan — Terraform apply steps, image build/push, Airbyte connection steps), `architectural_decisions.md` (ADR-001/003 — the original Docker Compose/local-Postgres decisions this plan changes the *hosting* of, not the app architecture itself), `to_do.md` (Phase 1 already names a Data Protection Impact Assessment with Legal as a real, not-yet-started item — directly relevant here, see below), `.env.example` (the exact config surface, now including the AWS-specific vars this added: `DJANGO_ALLOWED_HOSTS`, `DJANGO_HTTPS`)

## What's actually built so far (2026-09-09)

- **`Dockerfile`** + **`docker-entrypoint.sh`** — runs the app under `granian` (already a dependency, previously unconfigured). `collectstatic` runs at container start, not build time, since it needs real settings (`SECRET_KEY`, DB env vars) that only exist once ECS actually injects them.
- **`config/settings.py`** — three additive changes, all with defaults that preserve local dev exactly: `ALLOWED_HOSTS` now reads `DJANGO_ALLOWED_HOSTS` (comma-separated) instead of being hardcoded to `localhost`; `STATIC_ROOT`/`STORAGES` added for WhiteNoise (in-process static serving — no S3/CloudFront needed at this scale); a `DJANGO_HTTPS` toggle gates `SECURE_SSL_REDIRECT`/`SESSION_COOKIE_SECURE`/`CSRF_COOKIE_SECURE`/`SECURE_HSTS_SECONDS` together, off by default since the ALB has no TLS listener yet (turning these on before a cert exists would redirect-loop every request).
- **`requirements.txt`** — added `whitenoise`.
- **`infra/aws/`** — the full Terraform stack: VPC (2 public + 2 private subnets across 2 AZs), RDS Postgres 17 (Multi-AZ, fully private, `deletion_protection = true`), ECS Fargate running the app in public subnets behind a security group that trusts only the ALB (deliberately not the "private subnets + NAT Gateway" pattern — saves ~$32/month a single-instance app doesn't need), an ALB (HTTP only for now), ECR, IAM roles scoped tightly (execution role can only read the 2 secrets it needs; task role starts empty), and Secrets Manager for `DJANGO_SECRET_KEY`/the DB password. **Not yet run against real AWS** — `infra/aws/README.md` has the full apply sequence, including the still-open Airbyte VPC-peering step (needs OAF's VPC ID/CIDR, which isn't available here).
- 103 tests still passing locally after all of the above — nothing about local dev changed behavior.

## Why

2026-09-09: the local dev environment's WSL2/Docker virtual disk hit a real fault — the host C: drive was at 96% full (5.9GB free), and Postgres started throwing `FATAL: could not open file "base/16384/2601": Read-only file system` on basic reads. A container restart didn't clear it; `psql` itself failed inside the container with an `Input/output error` on exec, meaning the fault was in the WSL2 virtual disk layer, not just Postgres. Same underlying class of problem as the `MemoryError`/WSL2 crash from `to_do.md`'s 2026-09-01 entry — disk pressure this time, not RAM. Moving off a single local machine's disk removes this whole failure class.

## What's actually moving

Wimbi has **no separate frontend to move** — ADR-010 deliberately dropped the idea of one. The whole app is a single Django service that server-renders HTML, with HTMX/Tailwind/Chart.js loaded from CDN inside the templates (no Node.js, no build step, no separate frontend host or process). "Move the backend and frontend" in practice means: move this one Django app, and move its Postgres database.

**Current local shape** (`docker-compose.yml`):
- `wimbi_db` — Postgres 17, `wimbi`/`wimbi`/`wimbi`, real data: ~1.3M farmers, ~1.5M journey events, ~1.7M bridge rows, sales lines (5M target, partial load), ~10k SF employees, plus the `analytics_mirror` DimCountry/DimProgram/DimSystem/DimSeason tables and bulk_uploader's own operational tables.
- `wimbi_redis` — Redis 7, provisioned per ADR-001 for future Celery use — **nothing uses it today** (`REDIS_URL` is read but no task exists), so it doesn't need to move at all for now.
- Django app itself runs via `manage.py runserver` locally — there's no production WSGI/ASGI process defined yet. `granian` (a Rust-based server) is already in `requirements.txt` but has no configured entrypoint, systemd unit, or `Procfile` — that's a real gap to close as part of this move, not something already solved.

**This is real farmer PII** — names, ids, and (in some mirrored views) phone numbers for real Malawian farmers. That's the one point in this plan that isn't just a technical decision — see the open question below before anything gets provisioned.

## Proposed architecture

### Option A — minimal, single-instance (recommended to start)
- **1x EC2 instance** (`t3.micro` or `t3.small`) running the Django app directly (granian, behind a systemd unit so it survives a reboot) — no separate app server needed for one user.
- **1x RDS Postgres 17** (`db.t3.micro` or `db.t4g.micro`), replacing the docker-compose Postgres — security group open only to the EC2 instance, never public.
- **No Redis/ElastiCache** — skip it entirely for now; nothing depends on it.
- **Static files via WhiteNoise** (in-process, no S3/CloudFront needed at this traffic level) — one new dependency, `whitenoise`, and a small `MIDDLEWARE`/`STATICFILES_STORAGE` change.
- **No load balancer, no Multi-AZ, no auto-scaling** — single points of failure, accepted deliberately for a one-user dev/pilot environment. Access via SSH key pair + a security group locked to your own IP; the app itself stays behind Django's existing login, not exposed further.

### Option B — closer to "real" production (ECS Fargate + RDS + ALB)
Containerize (the existing `docker-compose.yml` shape maps fairly directly to ECS task definitions), put an Application Load Balancer in front, RDS Multi-AZ, credentials in Secrets Manager instead of a `.env` file on disk. Meaningfully more setup and cost — worth it if this becomes the permanent home rather than a stopgap for the local disk issue, not before.

**Recommendation:** start with Option A. It solves the actual problem (a broken local disk) with the least new surface area, and nothing about it blocks moving to Option B later — RDS in particular doesn't change shape between the two.

## Cost — honest answer: not free in general, and Airbyte is the number that actually matters here

For the app itself (EC2 + RDS, Option A):
- **A new-ish AWS account, within its first 12 months:** genuinely close to $0/month. The free tier covers 750 hrs/month of `t3.micro`/`t2.micro` EC2, 750 hrs/month of `db.t3.micro`/`db.t4g.micro` RDS with 20GB storage, and enough S3/data-transfer allowance that this scale of app wouldn't come close to the limit.
- **An account past its first 12 months:** roughly **$20–35/month** — EC2 ~$7–15/month, RDS ~$15–20/month, no load balancer or NAT Gateway (Option A deliberately avoids both — a NAT Gateway alone runs ~$32/month and isn't needed for a single public-subnet EC2 instance with a locked-down security group).
- **Option B** adds an ALB (~$16/month + usage) and Multi-AZ RDS (roughly doubles the RDS line) — meaningfully more, not recommended to start.
- Storage sizing is currently a guess — I can't check the real database's on-disk size until DB access is restored (that's the outage this move is partly in response to), so "does 20GB actually fit the real data" is unverified, not assumed-yes.

**Airbyte is the line item that can dominate the whole bill, regardless of free tier:**
- Self-hosted: realistically **$60+/month** just for the compute Airbyte itself needs (well past what free-tier EC2 sizes can run), on top of everything above.
- Airbyte Cloud: no compute cost, but usage-based pricing on rows synced — with multi-million-row fact tables refreshed periodically, this is a real, recurring, usage-driven cost that doesn't get smaller once the free trial ends.
- Either way, **Airbyte is very unlikely to be free**, even on a brand-new AWS account — this is the honest answer to "I don't know if it's free": the app hosting plausibly is (for 12 months), Airbyte plausibly isn't, from month one.

## The non-technical question: where should real farmer PII live?

**Partly answered already** — this is your own personal AWS account, a deliberate choice, not a default fallen into. What's still open is narrower but still real: if Airbyte Cloud is chosen over self-hosted, real farmer data (names, ids, phone numbers in some views) transits a third party's infrastructure on every sync, even briefly, rather than staying entirely inside AWS infrastructure you control end to end. `to_do.md`'s own Phase 1 checklist already names a Data Protection Impact Assessment with Legal as real, tracked, not-yet-started work — worth keeping in mind here, not as a blocker, just as context for the Airbyte hosting choice above.

## Decisions made (2026-09-09)

- **Account:** the user's own personal AWS account, not an OAF-managed one.
- **Permanence:** this is the permanent home for Wimbi going forward, not a stopgap — worth a bit more deliberateness even within a minimal starting shape. **Revised 2026-09-09 (later still)**: not permanent after all — this personal AWS account is a **~3-week bridge**. OAF's own DevOps team will provide a real domain under `*.oneacrefund.org` (separate `prd`/`qa`/`uat` subdomains) once this migrates into OAF's own AWS account. Doesn't undo anything already built — the Terraform stack, Dockerfile, and app-side changes all move over largely as-is — but it does change what's worth investing in *here* versus deferring to the real migration. SSL/domain is the first thing this changes: deferred entirely until the OAF domain exists, rather than buying a throwaway personal domain now.
- **Data path, split in two:**
  - **Wimbi's own operational tables** (`accounts`, `bulk_uploader` — the `public` schema, per ADR-006's schema isolation) migrate "as they are." In practice this is smaller than it sounds: `RoleAssignmentRule` is seeded by a Django **data migration** (ADR-008), and `auth.User`/sessions are re-provisioned just-in-time on login (ADR-009) — both recreate themselves automatically the moment `python manage.py migrate` runs on the new RDS instance, no manual export needed. The one thing that's genuinely irreplaceable real work is `bulk_uploader.UploadedDataset`/`UploadedRow` — the actual uploaded-and-mapped datasets (e.g. the 1,000-row Malawi registration file already saved). That's a small, targeted `pg_dump -t` (or a Django `dumpdata`/`loaddata` pair, which travels better across a Postgres version/schema difference) of just those two tables, once the local DB is readable again.
  - **`analytics_mirror`** does **not** get copied from local at all — it gets rebuilt properly via **Airbyte**, syncing directly from Snowflake into the new RDS instance. This is real, meaningful new scope: Airbyte was already named as future work in `jira-backlog.md` (EPIC P2: "Recurring sync job (Airbyte or scheduled...) — Not Started") and ADR-003 ("Open, not yet decided"); this migration is where it actually gets built, replacing the manual DBeaver CSV/direct-transfer approach used so far. See the Airbyte section below — it has its own real hosting/cost decision.

## Airbyte — hosting choice (this is a real decision, not a detail)

Two ways to run Airbyte, with meaningfully different cost and data-governance shapes:

| | Self-hosted (OSS) | Airbyte Cloud |
|---|---|---|
| **Where farmer data flows** | Stays entirely inside your own AWS account — Snowflake → your EC2/ECS → your RDS | Passes through Airbyte's own managed infrastructure in transit (their cloud, not yours) before landing in your RDS |
| **Compute** | Needs a real instance — Airbyte's own docs recommend ~4 vCPU/8GB RAM minimum, well past free-tier (`t3.micro` is 1 vCPU/1GB) — realistically a `t3.large` or bigger | None to run yourself — Airbyte's problem, not AWS's |
| **Cost shape** | AWS compute cost only, but not small: a `t3.large` runs roughly **$60/month** on-demand, before RDS/EC2-for-the-app on top | Free trial, then usage-based pricing on rows synced/month — with 1.3M+ farmers and multi-million-row fact tables refreshed periodically, this can add up fast, and it's billed separately from AWS |
| **Ops** | You own upgrades, connector maintenance, uptime | Managed for you |

**The real question underneath this table:** self-hosted keeps real farmer PII inside infrastructure you fully control end to end; Airbyte Cloud means that data transits a third party's systems, even briefly, on every sync. Given `to_do.md` already flags a Data Protection Impact Assessment as real, tracked, not-yet-started work, this is worth a deliberate answer, not a default — see the open question below.

## Migration steps (sequenced, blocked on the open questions below)

1. **Account + access** — an IAM user (not root) in the personal AWS account, least-privilege access to EC2/RDS/VPC (and Airbyte's own compute, once its hosting choice is made), MFA on, access keys never committed to the repo.
2. **Network** — a VPC (the AWS default VPC is fine to start), a security group for RDS (inbound Postgres from the app's and Airbyte's security groups only, never public), a security group for the app EC2 instance (inbound SSH from your IP only, HTTP/HTTPS as needed once there's a domain).
3. **RDS** — provision Postgres 17, note the endpoint, create the `wimbi` database/user matching `.env.example`'s shape, run `python manage.py migrate` against it (this alone recreates the schema and the role-assignment seed data — see above).
4. **`bulk_uploader` data** — once the local DB is readable again: `dumpdata`/`loaddata` (or a targeted `pg_dump -t`) of just `UploadedDataset`/`UploadedRow` onto the new RDS instance.
5. **Airbyte** — stand up per whichever hosting choice gets made below; configure the Snowflake source connector (credentials already exist in `.env.example`'s `SNOWFLAKE_*` block, just unused by the app itself — Airbyte is what actually reads them) and a Postgres destination pointed at the new RDS instance's `analytics_mirror` schema; set a sync cadence.
6. **App EC2** — provision the instance, install Python 3.12 + system deps, clone the repo (needs a deploy key or GitHub auth — the repo is presumably private), install `requirements.txt`.
7. **Django config for production** — real `DJANGO_SECRET_KEY` (generated fresh, not reused from local `.env`), `DJANGO_DEBUG=False`, `ALLOWED_HOSTS` set to the EC2 public DNS (or a real domain if one gets pointed at it), `collectstatic`.
8. **Run process** — a systemd unit running `granian` (already a dependency, currently unconfigured) so the app survives a reboot unattended; a domain + TLS is a later, separate decision, not required to get working.
9. **Verify** — run the `uat.md` acceptance pass against the AWS-hosted instance; confirm real row counts on RDS (both the `bulk_uploader` restore and the first Airbyte sync) match expectations before calling the move done.

## Decisions made (2026-09-09, continued)

- **Airbyte:** not provisioned by this project at all — **OAF already runs its own self-hosted Airbyte instance**, and the new RDS instance becomes a *destination* it syncs into, same as it presumably already syncs into other real infrastructure today. This removes the entire self-hosted-vs-Cloud compute/cost tradeoff above — no Airbyte compute to provision, own, or pay for here. What it does add: **RDS needs to be reachable from wherever OAF's Airbyte instance actually runs**, which is a real network question — see below.
- **App shape:** Option B — ECS Fargate + RDS (Multi-AZ) + ALB, production-shaped from the start rather than starting minimal.
- **Timing:** start provisioning now; the small `bulk_uploader` table migration (`UploadedDataset`/`UploadedRow`) happens as a follow-up once the local Postgres is readable again, not a blocker to starting.

## Open question: how does OAF's Airbyte actually reach the new RDS instance?

This is now the one real unresolved design point before RDS's network setup can be finalized. Options, depending on where OAF's Airbyte runs:
- **If OAF's Airbyte already runs in AWS** (even a different account) — VPC peering or a private connection is possible, keeping the connection off the public internet entirely. Needs OAF's AWS account/VPC details.
- **If it runs elsewhere** (on-prem, a different cloud, OAF's own servers) — RDS needs a public endpoint, locked down via security group to Airbyte's specific outbound IP(s) (not open to the internet generally), with SSL enforced (`sslmode=require` at minimum, ideally `verify-full`) and a strong dedicated Airbyte-only database credential (not the app's own Django DB user).

## Practical blocker before anything gets created

I don't have AWS credentials or the `aws` CLI available in this environment — nothing here can call the AWS API on your behalf right now. For work involving real billable resources in your own personal account, that's the right default anyway, not just a missing tool. Three ways to actually execute from here, in order of how much control you keep:

1. **I write Terraform** for the full Option B stack (VPC, RDS Multi-AZ, ECS Fargate, ALB, security groups) — you review it, run `terraform apply` yourself with your own credentials. Repeatable, reviewable, diffable for changes later. Recommended given the scope (production-shaped, permanent, real PII).
2. **I write exact AWS Console click-through steps** — no IaC, slower to redo/change later, but zero tooling needed on either side.
3. **You configure AWS CLI credentials in this environment** (`aws configure`, scoped to an IAM user with least-privilege access, not root) and I run commands directly — fastest, but means real AWS credentials sit in this dev environment.

## Live provisioning log (2026-09-09, via AWS CloudShell)

Chose option 1 above. Executed step by step, guided, in AWS CloudShell — a real run, not a dry one:

- **IAM:** CloudShell defaults to the account's **root** user, which is never right for day-to-day provisioning. Created a real IAM user (`wimbi-admin`, `AdministratorAccess` — broad, but a real accountable identity instead of root, a deliberate tradeoff for a solo-project personal account rather than hand-crafting least-privilege across VPC/RDS/ECS/ALB/ECR/IAM/Secrets Manager). Took two tries — the first `aws configure --profile wimbi` got garbled keys pasted in, caught via `aws configure list` showing clearly-truncated masked values, fixed with a fresh access key and a slower, deliberate paste.
- **Region:** CloudShell's own session was already in `eu-north-1` (Stockholm) — updated the Terraform's `aws_region`/`availability_zones` defaults to match, rather than the `us-east-1` this was originally written against.
- **Real farmer PII found in git history before the first push** — `data/raw/snowflake.sql` (committed weeks earlier, real names/loan amounts/districts pasted in from Snowflake schema-discovery work) would have been the very first thing to leave local control if pushed as-is. Backed up the full repo, used `git filter-repo` to strip the file from every commit, then re-added a redacted version (schema/`DESCRIBE VIEW` output only, no sample rows) as a fresh commit. Verified with a full-history grep for a real name before trusting it — zero hits. `data/raw/snowflake.sql`'s documentation value survives; the PII doesn't exist in git anywhere anymore.
- **Pushed to `https://github.com/fatix11/wimbi`** (private) for the first time, cloned into CloudShell via a fine-grained, read-only, repo-scoped Personal Access Token.
- **`terraform validate` caught a real bug**: `aws_security_group` description fields only allow a specific ASCII whitelist (no em-dashes, no apostrophes) — a genuine AWS API constraint, not a style preference. Fixed across all three security groups, pushed, re-pulled, re-validated clean.
- **Cost check before `apply`, at the user's request**: Option B in `eu-north-1`, running 24/7, came to roughly $87-90/month (RDS Multi-AZ ~$40, Fargate ~$22, ALB ~$19, the rest small) — against $100 in credits, about a month of runway. **Decided: `db_multi_az = false`** (as a `terraform.tfvars` override, not a change to the module's own default) after confirming it doesn't affect the actual usage pattern — weekly Airbyte batch writes tolerate Single-AZ's rare brief-outage risk fine, since a failed sync just reruns; Multi-AZ protects against something (continuous live traffic needing zero downtime) this deployment doesn't have yet. Drops the estimate to roughly $65-70/month.
- **First real `terraform apply` hit a `FreeTierRestrictionError`**: the account's default AWS "Free Plan" caps `backup_retention_period` below the `7` days this was hardcoded to. Made it a variable (`db_backup_retention_days`, default `1`) instead of a hardcoded value, documented the fallback (lower further, or upgrade the account plan in Billing) directly on the variable.
- **Confirmed, discussing the "why AWS" reasoning directly**: the local-storage/loading pain at 100M+ row scale is a real, valid piece of the justification, but the *ingestion* half of that pain is actually fixed by the Airbyte decision, not the AWS move itself — the two are complementary. Separately confirmed Airbyte could never have written into the local Docker-hosted Postgres anyway, regardless of row count: nothing outside the home network can open a connection in without port-forwarding (a real security exposure) or a tunnel, and a personal laptop isn't a dependable always-on sync target — reachability, not just scale, was always going to force this move.
- **Superset clarification**: Superset queries Snowflake directly, not Wimbi's `analytics_mirror` — so Superset's own "static" feeling is a separate, Snowflake-pipeline-side concern, out of scope here. The real ask was narrower: Wimbi's own copy (downloaded once via DBeaver, never refreshed since) silently drifting from what Superset shows live from the same underlying Snowflake data — exactly the risk ADR-007 already named. Airbyte syncing into the new RDS instance on a schedule is the fix once it's wired up. **Sync cadence confirmed: weekly (7 days) for now** — accepted as "good enough" given the actual current usage (aggregate numbers, not real-time decision-making), with an explicit plan to shorten once there are more real testers relying on freshness.

- **`apply` completed in two passes**, both real signal, not noise: a CloudShell terminal disconnect mid-run left `aws_ecs_task_definition.app`/`aws_ecs_service.app` uncreated (confirmed cleanly via `terraform state list` + a re-`plan` showing exactly those 2 resources pending, nothing duplicated); registering the task definition then failed on `container_image` containing literal `<account-id>` placeholder text — `Container.image contains invalid characters`, since ECS validates the image string's format at registration time, not just at pull time. Fixed by pulling the real value from `terraform output ecr_repository_url` (the ECR repo already existed from the first pass) into `terraform.tfvars`, then re-applying clean.
- **Image built and pushed from CloudShell** (Docker works there — confirmed early on), first real test of the `Dockerfile`/`docker-entrypoint.sh` — built clean on the first try. `force-new-deployment` picked it up; ALB health check on `/login/` went green (`200 OK`, real Django security headers, `server: granian`) — a genuinely live, working Wimbi instance at `wimbi-prod-alb-580559063.eu-north-1.elb.amazonaws.com`.
- **Migrations run via a one-off `ecs run-task`** (container-command override to `python manage.py migrate`, same image, no extra IAM needed — simpler than setting up ECS Exec for a single one-time command) — `exitCode: 0`. Same pattern for `ensure_analytics_tables` (creates `analytics_mirror`'s schema-only, empty tables — the same "Django defines schema, nothing else auto-creates tables" discipline as local, now applied on RDS too) — also `exitCode: 0`.

**State as of pausing here:** the AWS deployment is real, live, and functioning — VPC/RDS/ECS/ALB all up, the app serving over HTTP, Django's own schema migrated, `analytics_mirror`'s tables created and empty (ready for Airbyte). **Still open, explicitly deferred to resume later:** the `bulk_uploader` table migration (`UploadedDataset`/`UploadedRow` — local DB is healthy and this is unblocked, just needs its own careful pass given RDS has no public access, so the mechanics need thinking through, not squeezed in at the end of a long session); the Airbyte VPC-peering step (still needs OAF's VPC ID/CIDR); a real domain + TLS cert (currently HTTP-only); `enable_execute_command` if interactive task debugging is ever wanted later.

## SSL deferred; Airbyte connectivity opened instead (2026-09-09, resumed)

**SSL/domain:** deliberately deferred, not forgotten — the ~3-week-bridge revision above (OAF providing a real `*.oneacrefund.org` domain) makes buying a throwaway personal domain now pure waste. Revisit once that domain exists.

**Airbyte connectivity — the public-endpoint path chosen over VPC peering**, given the same bridge-period reasoning (not worth requesting a peering connection from OAF's side for infrastructure that gets rebuilt in three weeks anyway):

- **`db_publicly_accessible`** — new bridge-only Terraform variable (default `false`). Gives RDS a public IP and moves its subnet group to include the public subnets. Real snag hit on the first `apply`: swapping the subnet group straight from private-only to public-only failed — `Some of the subnets to be deleted are currently in use` — because AWS's `ModifyDBSubnetGroup` API refuses to remove a subnet the instance's ENI is actively attached to. Fixed by making the subnet group the *union* of both (`concat(public, private)`) rather than a swap, so nothing is ever removed while in use. Applied clean on the second pass; `PubliclyAccessible: true` confirmed directly via `aws rds describe-db-instances`.
- **Security group still gates actual access** — `publicly_accessible` alone doesn't open anything to the internet; the `aws_security_group.rds` rule for `airbyte_source_cidr` (still unset) is what will. **Still waiting on the real IP Airbyte connects from** before that rule gets added — until then, RDS has a public endpoint that nothing outside the VPC can actually reach.
- **Dedicated `airbyte` Postgres role created**, scoped to just the `analytics_mirror` schema (connect + usage + create + full privileges on existing and future tables/sequences there, via `ALTER DEFAULT PRIVILEGES` — Airbyte's Postgres destination can create its own tables for new streams) — deliberately not reusing the app's own master `wimbi` credential, which has full rights over everything including the app's own `bulk_uploader`/`accounts` tables. Created via the same one-off `ecs run-task` pattern as `migrate`, using Django's own DB connection with parameterized SQL (`cursor.execute(sql, [params])`) so the generated password never appears in a raw SQL string. `exitCode: 0`.

**Next when resuming:** get Airbyte's real outbound IP/CIDR, add it as `airbyte_source_cidr`, re-apply, then have OAF's Airbyte actually attempt the connection for the first time.

## Airbyte connected; landing-schema decision (2026-09-09, resumed again)

Airbyte's destination check and first real sync (`DIM_MCF`) both succeeded, after three real, separate fixes stacked on top of each other — worth naming distinctly since they'd otherwise blur into "network stuff":

- **RDS's ENI was in a private subnet with a public IP that had no route out** — inbound arrived fine (VPC-local routing), so the security group could reject or allow it, but the reply had nowhere to go, and the client just timed out (`08001`, indistinguishable from a firewall block). RDS also flatly refuses to move a running instance's subnet group within the same VPC (`ModifyDBInstance` treats that parameter as a cross-VPC move only — confirmed via two Terraform applies and the console's own modify flow, all failing identically). Fixed by routing the private route table to the IGW instead, gated on `db_publicly_accessible` — see `vpc.tf`.
- **The `airbyte` Postgres role had schema-level `CREATE` on `analytics_mirror` but not database-level `CREATE`** — needed because Destinations V2 stages into its own schema (`airbyte_internal`) that doesn't exist yet, and Postgres checks the permission before the existence check even for `CREATE SCHEMA IF NOT EXISTS`. Misdiagnosed once as a missing `CONNECT` grant (wrong guess — `CONNECT` is on `PUBLIC` by default and was never actually missing; the real tell was `ERROR`/unquoted-name severity, not the `FATAL`/quoted-name shape a real missing-`CONNECT` error takes). Fixed with `GRANT CREATE ON DATABASE wimbi TO airbyte`.
- **Confirmed via Adminer** (added as an optional bridge-period ECS service, gated on `enable_adminer`, reaching RDS by security-group reference rather than the public endpoint) that Destinations V2 preserves Snowflake's casing exactly (`DIM_MCF`, `MCF_ID`, ...) rather than folding it — meaning a direct sync into `analytics_mirror` would never have silently collided with the existing lowercase Django-created tables; at worst it would've created a parallel, differently-cased table.

**Schema split, decided:** `analytics_mirror_raw` is Airbyte's landing zone — it owns this schema fully, whatever naming/casing/bookkeeping columns (`_airbyte_raw_id`, `_airbyte_meta`, ...) it wants. `analytics_mirror` holds Postgres views over that raw data, doing the case/column translation to the exact names Django's `managed=False` models expect (e.g. `analytics_mirror.dim_mcf` as `SELECT "MCF_ID" AS mcf_id, ... FROM analytics_mirror_raw."DIM_MCF"`). This avoids a scheduled copy/transform job entirely — the view is a live pass-through, always current, no duplicated storage, no staleness window, no cron job that can fail or double-run.

This replaces `ensure_analytics_tables`' job for these tables: instead of creating empty tables for Airbyte to land into directly, it needs to create (or `CREATE OR REPLACE`) the views once the raw tables exist.

**One real unknown, not yet tested:** whether a full-refresh-**Overwrite** sync replaces the raw table in a way that survives a dependent view (Postgres refuses to drop a table with a dependent view except via `CASCADE` — if Airbyte's internal swap does that, the view breaks every sync). **Incremental** syncs are expected to be safe by construction (upsert into the existing table, never drop it), and most tables have keys suited to incremental — but this is still to be confirmed per-table, not assumed. Test empirically (trigger a second sync, check the view still resolves) before relying on this pattern for any table using Overwrite.

## Views built, mapping documented, Overwrite confirmed to break them (2026-09-09, end of day)

**Direct DB access opened up.** `db_admin_cidrs` — a new Terraform variable/security-group rule, deliberately separate from `airbyte_source_cidrs` since the two change on unrelated schedules (one changes when Airbyte's infra does, the other when the admin's own network does). Lets pgAdmin/DBeaver/Adminer connect with the master credential directly, instead of round-tripping every check through `ecs run-task` + reading CloudWatch logs — a real workflow upgrade for anything past this point. Caught and fixed mid-session: the CIDR went stale the first time the user's network changed, confirming the variable's own staleness warning wasn't hypothetical.

**All 13 mirror views built** (`infra/postgres/analytics_mirror_views.sql`) — one per `analytics_mirror` model except `sf_employees` (no raw table synced yet). Real decisions made building these, not just column renames:
- **Country-name-to-ISO normalization** (`country_codes.py`) replicated once as a Postgres function (`analytics_mirror.to_iso_country`) rather than duplicated across the four views that need it (`v_client_reach`, `v_client_journey`, `sales_line`, `bridge_client_source_ids`). No way to call the Python version from SQL — the two must be kept in sync by hand, called out directly in the function's comment.
- **Five views** (`v_client_journey`, `sales_line`, `loan_portfolio`, `program_summary`, `repayment_transaction`) have no natural key, so Django's schema_editor had originally given their (now-replaced) tables a real auto-increment `id`. A view can't fabricate that the same way — used a `ROW_NUMBER()` surrogate instead, verified safe first: `farmers/views.py` only ever fetches these scoped to one farmer, rendered once per request, `id` used as a list key — never paginated or deep-linked. One honest exception: `v_client_journey`/`sales_line` back Django admin's changelist, whose pagination could shuffle a tied row between two page loads — cosmetic, not a data-integrity issue.
- **Flagged, not fixed:** `V_SALES_DETAIL.LATITUDE`/`LONGITUDE` synced as `bigint` — real GPS decimals would be truncated. Cast to `float8` to match the model's field type, but whether real precision survived needs checking against actual values before trusting map display.

**A Wimbi↔Snowflake mapping table** (`infra/postgres/wimbi_analytics_mapping.sql`) — `public.wimbi_analytics_mapping`, columns `table_id`/`description`/`wimbi_name`/`analytics_db_name`/`is_similar`/`is_normalized`/`normalization_notes`. Consolidates what was previously scattered across each model's own "Mirrors V_X" docstring into one browsable table, covering all 21 known real objects (12 with views, `sf_employees` modeled-but-not-synced, 7 raw-synced-but-not-modeled dimension/client tables that map to the bulk-uploader glossary's future entities). Lives in `public`, not `analytics_mirror`, so it survives that schema's own churn. A docs-side mirror of this is deliberately deferred, not forgotten.

**The first full sync took ~3 hours and exposed a real scheduling problem**, not just "big tables are slow": small tables (`DIM_COUNTRY` — 29 rows, `DIM_SEASON` — 171, `DIM_SYSTEM` — 218) sat queued behind multi-million-row streams (`V_SALES_DETAIL`, `V_CLIENTS`, `DIM_CLIENT`) sharing the same connection's concurrency pool, not because they were slow themselves. Fixed by splitting into three Snowflake source connections by size tier, so tiny lookups stop queueing behind huge extracts. One-time cost either way — this is a full historical backfill; the incremental syncs already planned won't repeat it.

**The "one real unknown" from the last session is now answered, the hard way.** A stream running Full Refresh (Overwrite) failed with `Failed to drop table without the CASCADE option` — confirming Airbyte's internal swap really does issue a plain `DROP TABLE` on the raw table, which Postgres correctly refused because `analytics_mirror`'s view depends on it. Airbyte's suggested fix (enable `drop_cascade`) was rejected deliberately: it would make the sync succeed by silently dropping the dependent view along with it, on an unattended 7-day schedule, with no error anywhere pointing back at the cause — strictly worse than today's loud, visible failure. **The real fix: switch affected streams to Incremental** (Append+Deduped where a primary key exists, Append where only a cursor timestamp does) so the DROP codepath never runs at all. Not yet confirmed viable for every stream — `v_client_journey` in particular didn't show an obvious cursor column in its synced schema; needs checking per-stream in Airbyte's Schema tab before assuming this works uniformly.

**Verified against real, documented row counts, not just "non-zero":** once the smaller tables' first sync succeeded, `dim_program` = 29, `dim_season` = 171, `dim_system` = 218, `dim_mcf` = 7 — exact matches to counts hardcoded in `analytics_mirror/models.py`'s docstrings back when those models were first built against real data. Real proof the casing/column translation is correct, not assumed.

**Next when resuming:** confirm Incremental sync mode is viable per-stream (real primary key or cursor field) and switch the Overwrite streams over, starting with whichever one threw the CASCADE error; once the remaining large-table syncs finish, re-run the view row-count check and resume actual Wimbi feature work (search/dashboard) against real data; the lat/long precision check and the `bulk_uploader` data migration are both still open from earlier.
